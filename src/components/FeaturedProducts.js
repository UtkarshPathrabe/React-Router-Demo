const FeaturedProducts = () => {
    return (
        <div style={{ width: "100%", height: "60vh", display: "flex", alignItems: "center", justifyContent: "center", fontSize: '1rem' }}>
            List of Featured Products
        </div>
    );
};

export default FeaturedProducts;